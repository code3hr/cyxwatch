package com.cyxwatch.app.platform.network

object VpnModeCapabilities {
    // Intentionally false in V1. Keep centralized here so UI and service behavior stay aligned.
    const val FORWARDING_MODE_SUPPORTED = false
}

